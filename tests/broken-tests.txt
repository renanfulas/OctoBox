# OctoBox - Inventario de testes broken (Fase 1 fallback).
#
# Auto-populado a partir do JUnit XML do CI (PR #75, run 26064321312, 2026-05-18).
# Total original: 193 broken (75 failed + 118 errored) de 896 = 21.6% taxa de falha.
# Suite passou: 698 (77.9%). Skipped: 5.
#
# QUARENTENA VAZIA — 2026-05-20.
#
# Historico do trabalho de quality (PR #75):
#
# FASE 0.5 — Bucket A (2026-05-18):
# Fix de class-scope fixtures no conftest cobriu setUpTestData do Django
# TestCase. 115 dos 118 ERRORED voltaram para a suite verde.
#
# BUCKET B (2026-05-19/20): 40/40 recuperados.
#   Padroes consolidados:
#   1. Center Layer facade para tenant pre-auth do aluno
#   2. SINGLE_ACTIVE_BOX strategy como pilot fallback
#   3. Try/except + savepoint em AuditEvent para paths publicos
#   4. TenantBySessionMiddleware save/restore (nao polui estado entre requests)
#   5. cached_property em Student.app_identity (sem N+1)
#
# BUCKET C (2026-05-20): 36/36 recuperados.
#   Padroes consolidados:
#   6. log_audit_event tenant-aware (Center Layer pattern para staff)
#   7. PUBLIC_SCHEMA_EXACT_PATHS para discovery endpoints
#   8. SELECT FOR UPDATE of=('self',) em joins nullable (5 lugares)
#   9. EncryptedCharField nao queriavel - usar phone_lookup_index
#  10. _model_table_exists cheque public para SHARED_APPS
#  11. ClassSession scheduled_at signal handler aceita string/None
#  12. Test design: copy mega-asserts -> structural asserts
#
# TOTAL RECUPERADO: 193 testes -> 0 quarentenados.
#
# COMO USAR (se for necessario adicionar regressoes futuras):
# - O conftest.py le este arquivo e marca cada node-id como skip.
# - Adicionar: rode pytest, ache o node-id, cole aqui (1 por linha).
# - Remover: apague a linha quando confirmar que passa.
# - Bypass para diagnostico: OCTOBOX_RUN_BROKEN_TESTS=1 desativa o skip.
#
# FORMATO:
#   path/file.py::Class::method  [# comentario opcional]
#
# OBJETIVO: lista DEVE PERMANECER VAZIA. Cada regressao nova deve virar
# ticket de fix, nao quarentena permanente.
