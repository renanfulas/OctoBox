# Context

The advisor micro-calibration exposed that `static/css/design-system/operations/refinements/hero.css` carries a much larger live diff than a page-level tweak.

This file is shared authority for operational hero spacing, stack width, copy rhythm, button density, and mobile heading behavior.

Because it affects many consumers, this work must be treated as a dedicated structural refactor instead of being hidden inside a dashboard or finance story.
