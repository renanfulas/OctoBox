# Spec

## Regra

O runtime principal nao deve renderizar `page_compass`, `compass-pulse`, `pulse-chip` nem depender de `shell_action_buttons` para montar a pagina.

## Fora de escopo

Nao migrar o espelho `OctoBox/` nesta rodada; apenas classificar como legado se necessario.
